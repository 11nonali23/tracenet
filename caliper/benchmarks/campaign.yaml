test:
  name: basic test
  workers:
    type: local
    number: 1
  rounds: 
    - label: Create Campaign Fixed Rate (2000:100)
      txNumber: 2000
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/createCampaign.js
        arguments:
          assets: 500
          max: 10
          min: 3
          tx: 2000
          contractId: campaign

monitor:
  type:
  - none

observer:
  type: local
  interval: 5
